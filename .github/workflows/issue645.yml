name: issue645

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5
        
      - name: Setup Go
        uses: gowridurgad/setup-go@errorhandling-arch
        with:
          go-version: '1.18.0-rc.1'
          architecture: 'ppc64'
          
 

      
