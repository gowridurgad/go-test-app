name: issue645

on:
  workflow_dispatch:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        
      - name: Setup Go
        uses:  gowridurgad/setup-go@testing-ppc64
        with:
          go-version: '1.24.x'
          architecture: 'ppc64le'
          
